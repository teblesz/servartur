namespace servartur.DomainLogic;

public static class ResultsCalculator
{
    // for votes on quest tak einto acount that in quest == 4 and playersCount >=7 
    // you need 2 failure votes to fail quest
}
